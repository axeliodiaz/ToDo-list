# Example environment variables for docker-compose services
# Copy this file to .env and adjust values as needed for your environment.

# Database configuration
POSTGRES_DB=todo_db
POSTGRES_USER=todo_user
POSTGRES_PASSWORD=todo_password

# API configuration
# Default port exposed by the API inside the container
PORT=8000

# Connection string used by the API service (async)
DATABASE_URL=postgresql+asyncpg://todo_user:todo_password@db:5432/todo_db
